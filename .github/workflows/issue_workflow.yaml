  -name: second_workflow
   on:
     issues:
       branches:
         - master
   jobs:
      test:
       runs-on: ubuntu-latest
       steps:
         - name : echo context
           run: echo "{{github.event.issue.title}}"